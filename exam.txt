LOAD DATA INFILE '/path/to/Data.txt'
INTO TABLE TableName
FIELDS TERMINATED BY ',' ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS;

CREATE USER 'MA01'@'localhost' IDENTIFIED BY 'Pqr@&12';
GRANT INSERT, UPDATE ON College.* TO 'MA01'@'localhost';
FLUSH PRIVILEGES;

CREATE USER 'Staff01'@'localhost' IDENTIFIED BY 'Xyz@&12';
GRANT UPDATE ON College.Marks TO 'Staff01'@'localhost';
FLUSH PRIVILEGES;

REVOKE ALL PRIVILEGES, GRANT OPTION FROM 'MA01'@'localhost';
REVOKE ALL PRIVILEGES, GRANT OPTION FROM 'Staff01'@'localhost';
DROP USER 'MA01'@'localhost';
DROP USER 'Staff01'@'localhost';

start delimiter//

finisg procedure end//

delimiter ;


CREATE PROCEDURE CountMaleFemale()
BEGIN
    SELECT Semester, Section, 
           SUM(CASE WHEN Gender = 'Male' THEN 1 ELSE 0 END) AS MaleCount,
           SUM(CASE WHEN Gender = 'Female' THEN 1 ELSE 0 END) AS FemaleCount
    FROM Student
    GROUP BY Semester, Section;
END;

CREATE PROCEDURE InsertSubjectRecords()
BEGIN
    INSERT INTO Subject (Subcode, Title, Sem, Credits) VALUES
    ('10CS81', 'ACA', 8, 4),
    ('10CS82', 'SSM', 8, 4);
END;

CREATE TABLE Old_Marks (
    USN VARCHAR(10),
    Subcode VARCHAR(10),
    SSID VARCHAR(10),
    Test1 INT,
    Test2 INT,
    Test3 INT,
    Final INT
);

CREATE TRIGGER MoveToOldMarks
AFTER DELETE ON Marks
FOR EACH ROW
BEGIN
    INSERT INTO Old_Marks (USN, Subcode, SSID, Test1, Test2, Test3, Final)
    VALUES (OLD.USN, OLD.Subcode, OLD.SSID, OLD.Test1, OLD.Test2, OLD.Test3, OLD.Final);
END;

CREATE TRIGGER CorrectCredits
BEFORE INSERT ON Subject
FOR EACH ROW
BEGIN
    IF NEW.Credits < 3 THEN
        SET NEW.Credits = 3;
    END IF;
END;

BACKUP TABLE Lecturer TO '/path/to/backup';


